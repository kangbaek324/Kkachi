version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/domain/user/db"
    schema: "db/migrations"
    gen:
      go:
        package: "repository"
        out: "internal/domain/user/repository"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true

  - engine: "postgresql"
    queries: "internal/domain/wallet/db"
    schema: "db/migrations"
    gen:
      go:
        package: "repository"
        out: "internal/domain/wallet/repository"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true

  - engine: "postgresql"
    queries: "internal/domain/currency/db"
    schema: "db/migrations"
    gen:
      go:
        package: "repository"
        out: "internal/domain/currency/repository"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
